# ケモトレ 引き継ぎ仕様書 兼 作成指示書

**版**: v1.1（2026-07-12作成、同日更新: VOICEVOX音声組み込み完了を反映）
**読者**: このプロジェクトを引き継ぐClaude（新しいセッション）
**依頼主**: ととまる
**成果物の現状**: `kemono_kintore.html`（単体HTML・動作確認済み）を本書と一緒に受け取ること。これが唯一のソースコード。依頼主のPCには生成ツール一式（`kemotore_kit.zip` 展開済み: HTML+bat+ps1）と `voices/` フォルダ（WAV60個）が既にある。

---

## 0. このドキュメントの使い方（引き継ぎClaudeへ）

1. まず本書を全部読む。次に `kemono_kintore.html` を読んで現状把握する。
2. 作業は「§7 ロードマップ」の順に進めるのが基本。ただし依頼主の指示が最優先。
3. **変更のたびに必ずヘッドレスブラウザ（Playwright）でエラーチェック＋スクリーンショット確認**をしてから納品する。これまでもそうしてきた。
4. 納品はSendUserFileで届け、デスクトップ接続時はアーティファクト `kemono-kintore` を update_artifact で差し替える（既存ID。新規作成しない）。
5. 依頼主とのやりとりは日本語。カジュアルでOK、ただし技術的に正直に（できないことはできないと言う）。

---

## 1. プロジェクト概要

**名称**: ケモトレ（🐾）
**目的**: 自宅筋トレ専用アプリ。ケモノ（獣人）トレーナー3人から選んで一緒に鍛える。**モチベ維持が最重要テーマ**。依頼主はケモナーで、毎日使い続けたくなる「キャラとの関係性」を核に据える。
**形態**: 現在は単体HTMLファイル（ブラウザで開くだけ・依存なし・オフライン動作）。将来はPWA化 → その先にネイティブアプリ化の可能性。
**対象環境**: PC（Windows）とスマホ（iPhone Safari / Android Chrome）。縦長スマホ前提のレイアウト。
**デザイン**: ダークテーマ（紺系 #0e1220）、丸みのあるカードUI、日本語。

### 依頼主のPC環境（参考）
Intel i7-14650HX / 64GB RAM / RTX 5050 Laptop 8GB / Windows 64bit。VOICEVOXインストール済み。

---

## 2. キャラクター仕様（最重要・変更時は依頼主確認）

全員**雄（♂）**。子供向けの可愛い系ではなく、**ファーリー/ケモナー向けの大人の男性ケモノ**（筋肉質・ガタイが良い）が望まれている。参考画像として依頼主から「太めで強面のクマ」「筋肉質なオオカミ系」のイラストが提示された（成人向けだったため、アプリ内はトレーニングウェア着用の健全な表現に留める方針で合意済み）。

| id | 名前 | 種族 | 性格 | テーマ色 | 声の方向性 |
|----|------|------|------|---------|-----------|
| `cat` | シャドウ | クロネコ♂ | クール・ツンデレ気味・淡々・効率重視。「お前」呼び、一人称は出さない | #8b9dff | **少年っぽい高め** |
| `dog` | ラン | ゴールデン♂ | 優しい・褒めて伸ばす・人懐こい。「キミ」呼び、一人称「ボク」 | #ffbf5b | **普通の男性** |
| `bear` | ゴウ | ヒグマ♂ | 豪快・熱血・兄貴分。「お前/相棒」呼び、一人称「俺」。「ガハハ」と笑う | #ff7a6b | **低い低音** |

### セリフカテゴリ（現状、各キャラ3本ずつ＋ランクアップ5本＝各20本）
- `greet`（ホームでの挨拶）/ `start`（種目開始時）/ `during`（種目の途中）/ `finishSet`（種目完了時）/ `finishWorkout`（全メニュー完了時）
- `rankUp[1..5]`（親密度ランクアップ時の特別セリフ。ランクが上がるほど心を開いていく＝**関係性の進展がケモナー的なご褒美**。シャドウはツンから徐々にデレる、ランは親友宣言へ、ゴウは生涯の相棒宣言へ）

### キャラ表現の注意
- ランクアップのセリフは「距離が縮まる感」を大事に。恋愛表現までは行かず「絆・相棒」の範囲（依頼主から変更要望があれば従う）。
- 性的表現・裸表現はアプリ内では入れない（画像差し替え機能でユーザーが自分で入れるのは自由）。

---

## 3. 現状の実装（v1で完成済みの機能）

### 3.1 画面構成
- **ホーム**: レベル/EXPバー、連続日数（🔥ストリーク）、トレーナー選択（3カード、選択中は光る枠）、選択キャラの挨拶表示、メニュー選択（5種）
- **絆タブ**: 3人の親密度ランク・次ランクまでのpt・解放済み特別セリフの表示
- **記録タブ**: 累計回数/累計レップ/連続日数/最高連続 ＋ 実績バッジ10種（獲得/未獲得）
- **設定タブ**: 応援ボイスON/OFF、効果音ON/OFF、セット間休憩秒数（5〜120秒）、**トレーナー画像差し替え**（3人個別・「戻す」あり）、データリセット（2度押し確認）
- **ワークアウト実行画面**（全画面）: 進捗バー、キャラ立ち絵、吹き出しセリフ、種目名、回数/秒数の大表示、フォーム解説、完了ボタン or 自動タイマー、休憩画面（次種目予告つき）
- **リザルト画面**: 獲得EXP/親密度/レップ、レベルアップ演出、ランクアップ演出（特別セリフ表示）、新規バッジ、「もう1セット」

### 3.2 メニュー5モード
| モード | 内容 |
|--------|------|
| 初心者 全身 | 腕立て10・スクワット15・クランチ15・プランク30秒の固定4種 |
| 部位別 | 胸/腹/脚/背中/腕/肩/全身から選択 → その部位の種目最大4つ |
| 時短HIIT | 20秒運動を4種×2〜4周（種目はHIITプールからランダム） |
| おまかせ日替わり | 日付シードで毎日同じ4種が決まる（全種目プールから） |
| フリー | 全13種目から好きなものを選び回数/秒数も調整可能 |

種目データは `EX` オブジェクトに19種目。`type:'reps'`（回数系・完了ボタン式＋画面タップでカウント可）と `type:'time'`（秒数系・自動カウントダウン、残り3秒でビープ）。各種目にフォーム解説文つき。

### 3.3 成長システム
- **EXP/レベル**: 獲得EXP = 種目数×12 + レップ×0.6。必要EXP = 80+レベル×40
- **親密度**: 使ったトレーナーにのみ加算。獲得 = 種目数×15+30。ランク閾値: 0/100/300/600/1000/1600（初対面→顔見知り→仲間→相棒→親友→無二の存在）
- **ストリーク**: 日付ベース。前日から連続なら+1、飛んだら1にリセット。最高記録も保持
- **バッジ**: 10種（初回/累計10・50回/連続3・7・30日/Lv5・10/500レップ/親友ランク到達）

### 3.4 技術構成
- 依存ライブラリなし・純JS。状態は `localStorage`（キー `kemotore_v1`）に保存。localStorage不可時はメモリにフォールバック
- **音声**: Web Speech API（speechSynthesis）。日本語音声のうち男性名ヒント（ichiro/otoya/takumi等）を含むものを優先選択。キャラ別パラメータ: `VOICE={cat:{rate:1.02,pitch:1.30}, dog:{1.05,0.92}, bear:{0.95,0.45}}`
- **効果音**: Web Audio APIのサイン波ビープ（カウントダウン・完了音）
- **キャラ絵**: インラインSVG生成（`faceSVG()`=顔、`bodySVG()`=タンクトップ着用の筋肉質上半身）。`avatarMarkup(id)` が「カスタム画像があれば`<img>`、なければSVG」を返す
- **画像差し替え**: 設定画面からファイル選択 → canvasで最大512pxに縮小 → JPEG dataURLとして `state.customImg[id]` に保存。容量超過時は「今回のみ有効」トースト

### 3.5 state スキーマ（localStorage `kemotore_v1`）
```json
{
  "level": 1, "exp": 0,
  "totalWorkouts": 0, "totalReps": 0,
  "streak": 0, "bestStreak": 0, "lastDate": "2026-7-12",
  "affinity": {"cat": 0, "dog": 0, "bear": 0},
  "trainer": "cat",
  "badges": ["first"],
  "settings": {"voice": true, "sound": true, "rest": 30},
  "customImg": {"cat": "data:image/jpeg;base64,..."}
}
```
**互換性重要**: スキーマ変更時は必ず `Object.assign` 型のマイグレーションガードを追加し、既存ユーザーのデータを壊さないこと。

---

## 4. VOICEVOX 音声パイプライン（**組み込み完了済み**）

**背景**: Web Speechは端末依存（依頼主のPCは女性声しか入っていない）。キャラごとに本当に別の声にするため、依頼主PCのVOICEVOXで事前録音する方式で合意済み。クラウド環境からGitHubリリースが403でダウンロード不可だったため、**依頼主のPCで生成する**方式に確定。

### 4.1 生成ツール（配布済み・依頼主が実行済み）
`run_voices.bat` + `generate_voices.ps1`。VOICEVOX起動中にbatをダブルクリック → 同フォルダの `voices/` に60個のWAVを出力。
- 話者: cat=**12 白上虎太郎(ふつう)** / dog=**11 玄野武宏(ノーマル)** / bear=**13 青山龍星(ノーマル)**
- 調整: cat pitch+0.03 speed1.03 / dog 標準 / bear pitch-0.05 speed0.96
- ファイル命名規則: `{trainerId}_{category}_{index}.wav`（例 `cat_greet_0.wav`、`bear_rankUp_3.wav`）。**rankUpだけはidxがランク番号(1..5)**
- セリフ一覧は `voice_manifest.json`（60行、`{trainer,cat,idx,file,text}`）。**HTMLのTRAINERSのセリフ配列の順序＝ファイル名のidx**。この対応が命なので、セリフを編集/追加したら必ずps1とmanifestを再生成すること（作り方: HTMLからTRAINERSを抽出→カテゴリ×indexで列挙。前任セッションはnodeでevalして自動生成した）

### 4.2 アプリ側の再生機構（実装済み）
- `pickLine(cat)` がセリフ本文とWAVファイル名のペア `{text,file}` を返す（表示と音声が必ず同じセリフになる）
- `say(lineObj, next?)` が `voices/{file}` の `<audio>` 再生を試行、**404/失敗時は自動でspeechSynthesisにフォールバック**。`next` で連続再生（完走セリフ→ランクアップセリフのチェーンに使用中）
- `rankLine(id, rank)` がランクアップセリフ用のペアを返す
- 前提: **HTMLとvoicesフォルダが同じディレクトリにあること**（相対パス `voices/`）。スマホにHTML単体をコピーした場合はフォールバックで動く
- クレジット表記は設定画面下部に記載済み（話者を変えたらここも更新すること）

### 4.3 声の調整方法（依頼主から要望が来たら）
`generate_voices.ps1` 冒頭の3つの変数を書き換えて再実行してもらうだけ:
- `$spk`（話者ID。一覧は `http://127.0.0.1:50021/speakers`。例: もっと渋い低音なら青山龍星の「囁き」以外のスタイルIDや別キャラを試す）
- `$pitch`（-0.15〜+0.15が実用域。+で高く）
- `$speed`（0.8〜1.2が実用域）
再実行すると既存WAVは上書きされる。アプリ側の変更は不要（ファイル名が同じため）。話者を変えたらクレジット表記(§4.2)の更新を忘れない。

### 4.4 運用上の教訓（重要）
- `.bat`/`.ps1` を単体で送るとブラウザ/Windowsにダウンロードブロックされる。**必ずZIPに入れて送る**こと（`kemotore_kit.zip` 方式）
- PowerShell 5.1の `Invoke-WebRequest` は `-UseBasicParsing` を付けないとセキュリティ確認が出る（対処済み。新しくps1を生成するときも必ず付ける）

---

## 5. 新機能仕様A: セリフの飽き対策（依頼主の最新要望・最優先級）

依頼主の懸念:「毎日触るアプリだから同じセリフばかりだと飽きる」。AI自動生成も視野に入れているが、難しさは理解している。**3段階で実装する**。

### 5.1 第1段: セリフプール大拡充 ＋ 状況分岐（必須・まずこれ）
現状60本 → **300本以上**へ。単に増やすだけでなく**コンテキストタグ**で出し分ける:

```js
// セリフに条件タグを付ける設計例
{text:'朝から来るとは、いい心がけだ。', when:{hour:[4,10]}}          // 時間帯
{text:'7日連続……お前、本気なんだな。', when:{streakMin:7}}          // ストリーク
{text:'脚の日か。逃げないだけ偉いぞ。', when:{part:'脚'}}            // 部位
{text:'……久しぶりだな。心配した、わけじゃないが。', when:{daysAwayMin:3}} // 復帰時
{text:'お前とももう長い付き合いだな。', when:{rankMin:3}}            // 親密度
{text:'今日で50回目だぞ。よく続けてる。', when:{totalIs:50}}         // 節目
```
選択ロジック: 条件に合うセリフを絞る → **直近に言ったセリフを避ける**（直近履歴をstateに保持、`recentLines:{cat:[...]}` 最大20件）→ ランダム。これだけで日常の体感はほぼ「毎回違うことを言う」になる。

書くべきセリフの軸: 時間帯(朝/昼/夜/深夜)×3人、ストリーク節目(3/7/14/30日)、サボり復帰(2日/1週間ぶり)、親密度ランク別の口調変化（シャドウはランク4以降デレ度が上がる等）、部位別、季節(夏/冬)、レベル節目。**キャラの口調ブレに最注意**（§2の人称・語尾を厳守）。

### 5.2 第2段: 日替わり会話（テンプレート合成）
日付シード（既存の `daily` と同じ手法）で毎日1つ「今日のひとこと」を合成して見せる。`[挨拶部]+[話題部]+[締め部]` の組み合わせで数千パターン。ホーム画面のキャラ吹き出しに表示。**通信なし・確定的**なのでVOICEVOX音声化はできない（テキスト表示＋Web Speech読み上げにする）。

### 5.3 第3段: AI生成会話(オプション機能・上級者向け)
単体HTML/PWAから直接Claude APIを叩く構成は可能だが、**依頼主自身のAPIキーが必要**（キー埋め込み配布はNG、設定画面でユーザーが自分のキーを入力する方式のみ可）。
- 設定に「AI会話（実験的）」トグル＋APIキー入力欄
- 実装: ワークアウト完了時に `claude-haiku` 系の安価モデルへ「キャラ設定＋今日の実績」を渡し、リザルトの一言を生成。**システムプロンプトに§2のキャラ設定と口調規則を厳格に書く**
- コスト目安を依頼主に伝えること（1日数回の利用なら月数十円程度）
- 通信失敗時は第1段のプールにフォールバック
- **注意**: これは「あれば嬉しい」機能。第1段・第2段を終えてから着手

### 5.4 実装優先度の理由
録音ボイス(VOICEVOX)と第1段は相性が良い（セリフが固定だから録音できる）。AI生成セリフは録音できないのでWeb Speech頼みになる。つまり**「固定プール=キャラ声で喋る」「AI生成=おまけ」**という住み分けを依頼主に説明済みの方針として維持する。

---

## 6. 新機能仕様B: 豆知識システム（依頼主の最新要望）

要望:「トレーニングのコツや体にいい食べ物の豆知識を、**正確な内容で**キャラに話させたい」。

### 6.1 設計方針: 検証済み知識DB内蔵 ＋ キャラ口調変換
正確性が要件なので**AI生成にはさせない**。実装するClaudeが信頼できる知見に基づいて knowledge DB を執筆し、**同じ事実を3人分の口調で書き分ける**:

```js
const TIPS=[
 {
  id:'protein_timing', cat:'栄養',
  fact:'筋肉の材料になるたんぱく質は、トレーニング後だけでなく1日を通してこまめに摂るのが効果的（体重1kgあたり1.2〜2.0gが目安）',
  say:{
   cat:'たんぱく質は一度に摂っても使い切れない。朝昼晩、分けて摂れ。体重1kgあたり1.2から2グラムだ。',
   dog:'お肉やお魚のたんぱく質はね、まとめてじゃなくて毎食ちょっとずつ摂るのがコツなんだ!',
   bear:'肉だ肉ゥ!って一気食いはダメだぜ相棒。朝昼晩に分けて食うのが筋肉への近道だ!'
  }
 },
 // ...
]
```

### 6.2 コンテンツ執筆ルール（重要）
- 分野: ①フォームのコツ ②栄養（たんぱく質・炭水化物・水分・食事タイミング） ③回復（睡眠・休息日・超回復） ④よくある誤解の訂正（「筋肉痛=効いてる証拠ではない」等） ⑤安全（痛みがあるときは中止、など）
- **一般に確立した運動生理学・栄養学の範囲に限定**。数値を出すときは広く合意された目安のみ。医療アドバイスに踏み込まない（「持病がある場合は医師に相談」の姿勢）
- サプリの推奨・特定商品の推奨はしない。極端なダイエット・過度な追い込みを推奨する内容は書かない（依頼主の健康を守る）
- 初版は**50本**を目標。カテゴリバランスよく

### 6.3 表示タイミング
- **休憩画面**: 休憩中は暇なので豆知識の一等地。「💡 {キャラの口調のtip}」を吹き出しで
- **リザルト画面**: 1日1回、今日の豆知識
- **ホーム**: 日替わりひとこと（§5.2）と交代で出しても良い
- 既読管理（`state.tipsSeen:[]`）で未読優先、全部見たらランダム

### 6.4 音声化
豆知識も固定テキストなのでVOICEVOX録音対象にできる。ただし50本×3人=150ファイルで容量が嵩むため、**まずはテキスト表示のみ**とし、音声化は依頼主に容量と相談してから。

---

## 7. ロードマップ（推奨順）

| # | 作業 | 状態 | メモ |
|---|------|------|------|
| 1 | VOICEVOX音声の組み込み | **完了**（依頼主が60個生成済み・アプリ再生対応済み） | §4。残タスクは声の微調整要望への対応のみ(§4.3) |
| 2 | セリフプール拡充＋状況分岐 | 未着手・**次はこれ** | §5.1。拡充後はps1スクリプトとvoice_manifest.jsonも再生成して依頼主に再実行してもらう |
| 3 | 豆知識DB（50本）＋休憩/リザルト表示 | 未着手 | §6 |
| 4 | PWA化 | 未着手 | manifest.json / Service Worker / アイコン。音声WAVもキャッシュ対象に。ホスティングはGitHub Pages等を提案（無料・HTTPS必須要件を満たす） |
| 5 | 日替わり会話合成 | 未着手 | §5.2 |
| 6 | AI会話（オプション） | 未着手 | §5.3。依頼主の希望を再確認してから |
| 7 | ネイティブ化検討 | 保留 | Google Play登録$25(買い切り)/Apple $99年。コードは書けるがストア提出は依頼主の作業 |

## 8. 制約・既知の注意点

- **クラウド環境の制約**: GitHubリリースのダウンロードは403でブロックされる（VOICEVOXエンジンをクラウドに入れる案は失敗済み。再挑戦しないこと）。依頼主のPCへは device_bash / ファイルブリッジ経由でアクセス可能だがネットワークは繋がらない
- **localStorage上限**（約5MB）: カスタム画像3枚＋音声を全部入れるのは無理。音声はPWAのCache Storage or IndexedDBへ
- **iOSの自動再生制限**: 音声はユーザー操作起点でしか鳴らない。既存コードに「初回タップでAudioContext resume」処理あり。audio要素の再生も同様の配慮が必要
- **Web Speechの女性声問題**: 依頼主PCには男性日本語音声が未インストールの可能性。Microsoft Ichiroの追加を案内済み。根本解決はVOICEVOX録音（進行中）
- **日付処理**: `todayStr()` はゼロ埋めなし（"2026-7-12"形式）。変更すると既存ストリークが壊れるので触らない
- **VOICEVOXクレジット表記**を音声組み込み時に忘れない（§4）
- 依頼主は開発者ではない。専門用語は噛み砕き、実行してもらう作業は「ダブルクリックだけ」レベルまで簡単にする

## 9. 動作確認チェックリスト（納品前に毎回）

1. Playwrightでページを開いて **JSエラーゼロ** を確認
2. ホーム/ワークアウト/リザルト/絆/設定のスクリーンショットを撮って目視確認
3. 一連のワークアウト完走 → state（EXP/親密度/ストリーク/バッジ）が正しく更新されるか
4. 旧スキーマのstateを注入してもマイグレーションで壊れないか
5. ビューポート420×860（スマホ相当）でレイアウト崩れなし
